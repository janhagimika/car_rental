package com.example.car_rental.models;

public enum ReturnCondition {
    EXCELLENT,   // Car returned in perfect condition
    GOOD,        // Car returned with minor issues
    DAMAGED,     // Car returned with visible damage
    UNUSABLE     // Car returned in a state where it cannot be used
}
